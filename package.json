{
  "name": "my-simple-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx puppeteer browsers install chrome && node server.js"
  },
  "dependencies": {
    "express": "^4.21.1",
    "puppeteer": "^23.5.2"
  },
  "description": ""
}
